version: '3.8'
services:
  deepsproxy:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - API_KEY=${API_KEY:-}
    volumes:
      - ./deepseek_profile:/app/deepseek_profile
    restart: unless-stopped
